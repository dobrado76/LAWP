{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 4,
  "estimatedMinutes": 16,
  "skillIds": [
    "js.values"
  ],
  "id": "strings-and-templates",
  "courseId": "values",
  "moduleId": "kinds",
  "title": "Strings and templates",
  "blocks": [
    {
      "type": "explain",
      "md": "## Backticks insert a value\n\nOrdinary quotes keep `${name}` as letters — the dollar, the braces, and the word name. A **template literal** uses backticks. Only then does `${name}` become the value.\n\nThis is a different idea from last lesson. Immutability said a method returns a new string. Interpolation says *how* you build that new string when a name has to sit inside it. Quotes will not do that job, even if the text looks like a template.\n\n```javascript\nconst name = \"north\"\n\"beacon:${name}\"   // the characters beacon:${name}\n`beacon:${name}`   // beacon:north\n```\n\nA template builds a new label. It does not rewrite `name`. `tagBeacon` must use backticks so `tagBeacon(\"north\")` is `beacon:north`."
    },
    {
      "type": "predict",
      "id": "quotes-vs-ticks",
      "promptMd": "What does `\"beacon:${name}\"` return when `name` is `\"north\"`?",
      "kind": "mcq",
      "choices": [
        {
          "id": "lit",
          "md": "The characters `beacon:${name}` — quotes do not interpolate"
        },
        {
          "id": "val",
          "md": "`\"beacon:north\"`"
        }
      ],
      "answer": "lit",
      "explainMd": "Double quotes (and single quotes) do not run `${…}`. You get the dollar, the braces, and the word name. Backticks are what insert the value."
    },
    {
      "type": "check",
      "id": "tick-insert",
      "kind": "cloze",
      "promptMd": "Interpolation needs {{a}}. `\"beacon:${name}\"` is {{b}}.",
      "blanks": [
        {
          "id": "a",
          "choices": [
            {
              "id": "backticks",
              "md": "backticks"
            },
            {
              "id": "double quotes",
              "md": "double quotes"
            },
            {
              "id": "plus signs",
              "md": "plus signs"
            }
          ]
        },
        {
          "id": "b",
          "choices": [
            {
              "id": "literal text",
              "md": "literal text"
            },
            {
              "id": "beacon:north",
              "md": "beacon:north"
            },
            {
              "id": "an error",
              "md": "an error"
            }
          ]
        }
      ],
      "answer": {
        "a": "backticks",
        "b": "literal text"
      },
      "explainMd": "Only a template literal (backticks) replaces ${name}. Quotes keep those characters as text."
    },
    {
      "type": "code",
      "id": "tag-name",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> `tagBeacon(name)` must return a template literal: backtick `beacon:` then `${name}` backtick — so `tagBeacon(\"north\")` is `beacon:north`. Quotes will print the letters `${name}`. Do not hard-code `\"beacon:north\"`. Print `tagBeacon(\"north\")`.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "beacon:north"
        },
        {
          "type": "ast",
          "query": "`"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "If you used double quotes, `${name}` stays letters. Switch the string to backticks."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "A template is wrapped in backticks. `${name}` drops the argument in."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "With `kind = \"low\"`, backtick `signal:${kind}` backtick builds `signal:low`."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "function tagBeacon(name) { return `beacon:${name}` }\nconsole.log(tagBeacon(\"north\"))\nmodule.exports = { tagBeacon }"
        }
      ]
    }
  ],
  "description": "Backticks insert a value into a new string. Quotes keep the dollar signs as letters."
}
