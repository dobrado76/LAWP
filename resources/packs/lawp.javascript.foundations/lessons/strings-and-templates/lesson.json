{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 18,
  "skillIds": [
    "js.values"
  ],
  "id": "strings-and-templates",
  "courseId": "values",
  "moduleId": "kinds",
  "title": "Strings and templates",
  "blocks": [
    {
      "type": "explain",
      "md": "## Text does not mutate in place\n\n`\"east\".toUpperCase()` returns a **new** string. The original stays `\"east\"`. A beacon tag is the same idea: you build a new label; you do not scratch the old letters off.\n\nA template literal drops a value into a string: `` `beacon:${name}` ``. The result is still text."
    },
    {
      "type": "predict",
      "id": "string-immutable",
      "promptMd": "After `let d = \"east\"; d.toUpperCase()`, what is `d`?",
      "kind": "mcq",
      "choices": [
        {
          "id": "EAST",
          "md": "`\"EAST\"`"
        },
        {
          "id": "east",
          "md": "`\"east\"` — the method returned a new string"
        }
      ],
      "answer": "east"
    },
    {
      "type": "code",
      "id": "tag-name",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> `tagBeacon(\"north\")` returns `beacon:north`. Print that.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "beacon:north"
        },
        {
          "type": "ast",
          "query": "`"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "The starter hands the name back unchanged. You need the name inside a longer label."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "A template literal is wrapped in backticks and drops a value in with `${ }`."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "`` `signal:${kind}` `` with `kind = \"low\"` builds `signal:low`."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "function tagBeacon(name) { return `beacon:${name}` }\nconsole.log(tagBeacon(\"north\"))\nmodule.exports = { tagBeacon }"
        }
      ]
    }
  ],
  "description": "Text can be a plain string or a template that fills in values."
}
