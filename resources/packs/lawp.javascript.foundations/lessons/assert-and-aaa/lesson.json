{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 20,
  "skillIds": [
    "js.test"
  ],
  "id": "assert-and-aaa",
  "courseId": "craft",
  "moduleId": "prove",
  "title": "Arrange, act, assert",
  "blocks": [
    {
      "type": "explain",
      "md": "## AAA on a route helper\n\n**Arrange** a list of dirs. **Act** by calling `walkLength`. **Assert** with `assert.strictEqual`. A failed assert throws. The process exits non-zero — it does not only print a sad line. The fox walked two headings. A test that only `console.log`s `2` can lie. `assert` stops the run when the length is wrong.\n\nWhy it bites: printing `pass` with a broken function still looks green on your screen. The hidden test also calls `walkLength`. If you skip the assert, you have a notebook, not a proof. Arrange a case you know, act, then assert the number.\n\n```javascript\nconst assert = require(\"assert\")\nfunction walkLength(dirs) { return dirs.length }\nassert.strictEqual(walkLength([\"east\", \"south\"]), 2)\n// throws if the length is not 2\n```\n\nThe common mistake is `return 0` and still printing `pass`. On Try, implement `walkLength`. Assert a case you know. Then print `pass`."
    },
    {
      "type": "predict",
      "id": "assert-fail",
      "promptMd": "A failed `assert.strictEqual` …",
      "kind": "mcq",
      "choices": [
        {
          "id": "log",
          "md": "Only prints"
        },
        {
          "id": "throw",
          "md": "Throws, so the process exits non-zero"
        }
      ],
      "answer": "throw"
    },
    {
      "type": "check",
      "id": "assert-still-zero",
      "kind": "tf",
      "promptMd": "A failed `assert.strictEqual` only prints and the process still exits 0.",
      "answer": "false",
      "explainMd": "A thrown assert fails the process. That is how the grader knows the check did not hold."
    },
    {
      "type": "code",
      "id": "aaa",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> Implement `walkLength`. Assert it. Print `pass`.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "pass"
        },
        {
          "type": "ast",
          "query": "assert"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "`walkLength` returns how many headings are in the list."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "Call `assert.strictEqual` on a case you know, then print `pass`."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "const assert = require(\"assert\"); assert.strictEqual(walkLength([\"east\"]), 1)"
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "const assert = require(\"assert\")\nfunction walkLength(dirs) { return dirs.length }\nassert.strictEqual(walkLength([\"east\", \"south\"]), 2)\nconsole.log(\"pass\")\nmodule.exports = { walkLength }"
        }
      ]
    }
  ],
  "description": "Arrange the world, act, then assert what must be true."
}
