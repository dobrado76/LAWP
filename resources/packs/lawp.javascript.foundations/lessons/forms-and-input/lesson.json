{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 18,
  "skillIds": [
    "js.events",
    "js.dom"
  ],
  "id": "forms-and-input",
  "courseId": "the-page",
  "moduleId": "live",
  "title": "input event and value",
  "blocks": [
    {
      "type": "explain",
      "md": "## The live value\n\nThe `input` event fires as the person types. Read `event.target.value` (or the input’s `.value`). That string is the live field, not `innerHTML`.\n\nFor a whole form at once, `new FormData(formEl)` collects every named control: `data.get(\"name\")` reads one field and `Object.fromEntries(data)` turns the lot into a plain record. Controls without a `name` attribute are not collected, which is the usual reason a field goes missing.\n\nCopy it into `#echo`. On the echo plate, each letter the fox types should appear on the line below as it is typed."
    },
    {
      "type": "predict",
      "id": "value-field",
      "promptMd": "The text in a text field lives on…",
      "kind": "mcq",
      "choices": [
        {
          "id": "html",
          "md": "`innerHTML` of the input"
        },
        {
          "id": "value",
          "md": "`.value`"
        }
      ],
      "answer": "value"
    },
    {
      "type": "check",
      "id": "value-html",
      "kind": "tf",
      "promptMd": "The text in a text field lives on `innerHTML`.",
      "answer": "false",
      "explainMd": "A text input holds its string on .value. innerHTML is the wrong place to read it."
    },
    {
      "type": "code",
      "id": "echo-input",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> Mirror `#name` into `#echo` on `input`.",
      "preview": {
        "kind": "iframe"
      },
      "files": [
        {
          "path": "files/index.html",
          "role": "fixture"
        },
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "Listen for `input` on `#name`."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "Read `e.target.value` each time."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "Write that string into `#echo` with `textContent`."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "document.querySelector(\"#name\").addEventListener(\"input\", (e) => {\n  document.querySelector(\"#echo\").textContent = e.target.value\n})"
        }
      ]
    }
  ],
  "description": "Read what the learner typed as they type it."
}
