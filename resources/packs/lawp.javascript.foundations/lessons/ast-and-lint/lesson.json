{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 20,
  "skillIds": [
    "js.test"
  ],
  "id": "ast-and-lint",
  "courseId": "craft",
  "moduleId": "prove",
  "title": "A stand-in for a linter",
  "blocks": [
    {
      "type": "explain",
      "md": "## Refuse `==` in the edit file\n\nLAWP’s `ast` check is a **substring** search, not a real parser pass. Treat it as a stand-in for a linter. The file text must contain `===` and must not use `==`. The desk once filed `0` (lamp off) as the same as a missing name `\"\"` because someone wrote `==`. The fox route test uses `===` so off and missing stay different.\n\nWhy it bites: this is not ESLint. The grader looks at the file text. `==` converts before it compares, so `0 == \"\"` is true. `===` keeps the kinds apart. A stand-in linter that bans `==` is how this lesson makes that habit visible.\n\n```javascript\n0 == \"\"    // true  — converted\n0 === \"\"   // false — different kinds\nsame(0, \"\")  // must be false via ===\n```\n\nThe common mistake is leaving `==` in the starter. On Try, `same(0, \"\")` is false via `===`. Print `false`."
    },
    {
      "type": "predict",
      "id": "ast-honest",
      "promptMd": "This lesson’s `==` ban is…",
      "kind": "mcq",
      "choices": [
        {
          "id": "eslint",
          "md": "A full ESLint run"
        },
        {
          "id": "substr",
          "md": "A stand-in: the file text must not use `==`"
        }
      ],
      "answer": "substr"
    },
    {
      "type": "check",
      "id": "ast-is-eslint",
      "kind": "tf",
      "promptMd": "This lesson runs a full ESLint pass on your file.",
      "answer": "false",
      "explainMd": "The grader looks for === in the file text and rejects ==. That is a stand-in, not ESLint."
    },
    {
      "type": "code",
      "id": "no-double",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> `same(0, \"\")` is false via `===`. Print `false`.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "false"
        },
        {
          "type": "ast",
          "query": "==="
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "`==` would treat `0` and `\"\"` as the same. This lesson refuses that."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "Return `a === b`. Print `same(0, \"\")`."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "Use === only."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "function same(a, b) { return a === b }\nconsole.log(same(0, \"\"))\nmodule.exports = { same }"
        }
      ]
    }
  ],
  "description": "A linter reads the shape of the code, not only the text."
}
