{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 3,
  "estimatedMinutes": 22,
  "skillIds": [
    "js.proto"
  ],
  "id": "prototypes-chain",
  "courseId": "internals",
  "moduleId": "objects-deep",
  "title": "The prototype chain",
  "blocks": [
    {
      "type": "explain",
      "md": "## Shared methods live on the prototype\n\nThe **prototype chain** is where JavaScript looks next when an object does not own a field. `Object.getPrototypeOf(obj)` is that next place. Many objects share one method object. That link is `[[Prototype]]`, not a copy of the function on every instance. Two foxes from the same prototype share **one** `go` function: `a.go === b.go` is `true`.\n\nThe desk does not copy `move` onto every route card. Each card looks up `go` on the shared prototype when you call it. If you paste `go` onto each object, you waste memory and `a.go === b.go` becomes false — the night log then thinks you have two different walks. `Object.create(proto)` is how you hang two children on one method object without `new`.\n\n```javascript\nconst proto = { go() { return \"east\" } }\nconst a = Object.create(proto)\nconst b = Object.create(proto)\na.go === b.go  // true — one function on the prototype\na.go()         // \"east\" — looked up on proto\n```\n\nA common mistake is to `return true` from `sharedGo` without building the share, or to put `go` on each object as an own field. On Try, `sharedGo()` makes two objects that share one `go` via `Object.create`, returns whether `a.go === b.go`, and prints that `true`."
    },
    {
      "type": "predict",
      "id": "shared-method",
      "promptMd": "Two objects from the same prototype share…",
      "kind": "mcq",
      "choices": [
        {
          "id": "own",
          "md": "A private copy of each method"
        },
        {
          "id": "one",
          "md": "One method function on the prototype"
        }
      ],
      "answer": "one"
    },
    {
      "type": "check",
      "id": "proto-share",
      "kind": "cloze",
      "promptMd": "Two objects from one prototype share {{a}}.",
      "blanks": [
        {
          "id": "a",
          "choices": [
            {
              "id": "one method function",
              "md": "one method function"
            },
            {
              "id": "a private copy each",
              "md": "a private copy each"
            },
            {
              "id": "no methods",
              "md": "no methods"
            }
          ]
        }
      ],
      "answer": {
        "a": "one method function"
      },
      "explainMd": "The method lives once on the prototype. Instances look it up; they do not each get a copy."
    },
    {
      "type": "code",
      "id": "shared-go",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> `sharedGo()` makes two objects that share one `go` on a prototype (`Object.create`). Return whether `a.go === b.go` (`true`). Print it. Do not hard-code `return true`.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "true"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "Put `go` on one prototype object. Make two children with `Object.create`."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "Compare the two method functions: `a.go === b.go`."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "They should be the same function, not two copies."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "function sharedGo() {\n  const proto = { go() { return \"east\" } }\n  const a = Object.create(proto)\n  const b = Object.create(proto)\n  return a.go === b.go\n}\nconsole.log(sharedGo())\nmodule.exports = { sharedGo }"
        }
      ]
    }
  ],
  "description": "A missing field is looked up on the object behind this one."
}
