{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 22,
  "skillIds": [
    "js.arrays"
  ],
  "id": "arrays-reduce-once",
  "courseId": "data",
  "moduleId": "lists",
  "title": "One justified reduce",
  "blocks": [
    {
      "type": "explain",
      "md": "## reduce when you are building one value\n\n`map` and `filter` already make lists. `reduce` is for **one** accumulator that walks the row. Here that value is a path string: `[\"east\", \"south\"]` becomes `\"east-south\"`. The second argument to `reduce` is the seed. `[\"east\"].reduce((acc, d) => acc + d, \"\")` starts `acc` as `\"\"`, not as `\"east\"`.\n\nWhy it bites: `join(\",\")` is the easy answer and the starter already does it, so a desk files `east,south` when the signal board expected dashes. Hiding a `for` loop inside `reduce` for style also misses the point — you are building one string, not proving you know the method name.\n\n```javascript\n[\"east\", \"south\"].reduce((acc, d) => {\n  return acc ? acc + \"-\" + d : d\n}, \"\")\n// seed is \"\"\n// after east: \"east\"\n// after south: \"east-south\"\n```\n\n`pathOf([\"east\", \"south\"])` must return `east-south` using `reduce`. Print it. If the list has one heading, the answer is that heading with no dash. The hidden test also calls `pathOf([\"west\"])`."
    },
    {
      "type": "predict",
      "id": "reduce-seed",
      "promptMd": "`[\"east\"].reduce((acc, d) => acc + d, \"\")` starts `acc` as…",
      "kind": "mcq",
      "choices": [
        {
          "id": "east",
          "md": "`\"east\"` — the first item"
        },
        {
          "id": "empty",
          "md": "`\"\"` — the seed you passed"
        }
      ],
      "answer": "empty"
    },
    {
      "type": "code",
      "id": "join-path",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> `pathOf([\"east\", \"south\"])` returns `east-south` using `reduce`. Print it.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "east-south"
        },
        {
          "type": "ast",
          "query": "reduce"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "The starter joins with a comma. The lesson asks you to build the same idea with an accumulator."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "`reduce` carries one value across the list: the running path so far, plus the next direction."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "If the accumulator is still empty, the answer is just the direction; otherwise add `\"-\"` before it."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "function pathOf(dirs) {\n  return dirs.reduce((acc, d) => (acc ? acc + \"-\" + d : d), \"\")\n}\nconsole.log(pathOf([\"east\", \"south\"]))\nmodule.exports = { pathOf }"
        }
      ]
    }
  ],
  "description": "Fold a list into one value, and be able to say why."
}
