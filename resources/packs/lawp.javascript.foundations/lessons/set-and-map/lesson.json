{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 3,
  "estimatedMinutes": 22,
  "skillIds": [
    "js.arrays",
    "js.objects"
  ],
  "id": "set-and-map",
  "courseId": "data",
  "moduleId": "records",
  "title": "Set and Map",
  "blocks": [
    {
      "type": "explain",
      "md": "## Unique names, keyed lookup\n\nA `Set` keeps each value once. `new Set([\"east\", \"east\", \"south\"])` has size `2`. First-seen order is kept. A `Map` stores a value under a key you choose — not only a string field on an object. `map.get(\"north\")` is `undefined` when the key was never set. It does not throw. `map.has` is the honest test.\n\nWhy it bites: joining a raw log files `east-east-south` when the board only fired two headings. Looking up a missing beacon with a throw crashes the desk; treating the missing key as the key string itself (the starter) files `\"ghost\"` as if it were a real name.\n\n```javascript\nnew Set([\"east\", \"east\", \"south\"]).size  // 2\nconst beacons = new Map([[\"n\", \"north\"]])\nbeacons.get(\"n\")      // \"north\"\nbeacons.get(\"ghost\")  // undefined\nbeacons.has(\"ghost\")  // false\n```\n\nTwo jobs. `unique(dirs)` uses a `Set` and returns unique headings joined with `-`. `nameOf(map, key)` uses `Map` `.get` / `.has` and returns the value, or `\"none\"` when the key was never set. Print `unique([\"east\",\"east\",\"south\"])` (`east-south`)."
    },
    {
      "type": "predict",
      "id": "set-size",
      "promptMd": "`new Set([\"east\", \"east\", \"south\"]).size` is…",
      "kind": "mcq",
      "choices": [
        {
          "id": "3",
          "md": "`3` — it kept every push"
        },
        {
          "id": "2",
          "md": "`2` — the second east was already there"
        },
        {
          "id": "1",
          "md": "`1`"
        }
      ],
      "answer": "2"
    },
    {
      "type": "check",
      "id": "map-missing",
      "kind": "tf",
      "promptMd": "`map.get(\"ghost\")` throws if the key is missing.",
      "answer": "false",
      "explainMd": "get returns undefined. has tells you whether the key exists."
    },
    {
      "type": "code",
      "id": "unique-join",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> Two jobs. `unique(dirs)` uses a `Set` and returns unique headings joined with `-`. `nameOf(map, key)` uses `Map` `.get` / `.has` and returns the value, or `\"none\"` when the key was never set. Print `unique([\"east\",\"east\",\"south\"])` (`east-south`).",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "east-south"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "A Set answers \"which headings appeared\"; a Map answers \"what is stored under this key\". You need one of each."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "`new Set(array)` drops duplicates and keeps first-seen order; `[...set]` turns it back into a list you can join."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "`map.has(key)` is the honest test before `map.get(key)`, because a missing key gives `undefined` rather than throwing."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "function unique(dirs) {\n  return [...new Set(dirs)].join(\"-\")\n}\nfunction nameOf(map, key) {\n  return map.has(key) ? map.get(key) : \"none\"\n}\nconsole.log(unique([\"east\", \"east\", \"south\"]))\nmodule.exports = { unique, nameOf }"
        }
      ]
    }
  ],
  "description": "A set keeps each value once. A map stores a value under a key you choose."
}
