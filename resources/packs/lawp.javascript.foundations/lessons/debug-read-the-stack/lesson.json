{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 18,
  "skillIds": [
    "js.errors"
  ],
  "id": "debug-read-the-stack",
  "courseId": "errors",
  "moduleId": "recover",
  "title": "Debug: read the stack",
  "blocks": [
    {
      "type": "explain",
      "md": "## The top line is the throw\n\nA **stack trace** lists function calls from newest to oldest. The first `at` line after the message is usually where `throw` ran, not the first function in the file. Read that line. Fix that function. The rest of the stack is how you got there — useful, but not the crash site.\n\nThe fox was told `\"east\"` and still crashed. The stack’s first `at` line points at `step`. Someone left a blanket `throw` in that function, so a legal compass word dies the same way `\"up\"` should. The walk never prints `ok`, and the beacon stays dark. A debug desk that chases the file’s first function wastes the night on the wrong room.\n\n```javascript\nfunction step(dir) {\n  if (dir !== \"east\" && dir !== \"west\" && dir !== \"north\" && dir !== \"south\") {\n    throw new Error(\"bad\")\n  }\n  return dir  // \"east\" is legal — do not throw\n}\nstep(\"east\")  // returns \"east\"\n// step(\"up\")  // still throws\n```\n\nA common mistake is to throw on every call, or to remove the throw so `\"up\"` becomes legal too. On Try, fix `step` so `step(\"east\")` does not throw and you print `ok`. `step(\"up\")` must still throw."
    },
    {
      "type": "predict",
      "id": "stack-top",
      "promptMd": "The first `at` line after the message is usually…",
      "kind": "mcq",
      "choices": [
        {
          "id": "main",
          "md": "The first function in the file"
        },
        {
          "id": "throw",
          "md": "The function that threw"
        }
      ],
      "answer": "throw"
    },
    {
      "type": "check",
      "id": "stack-first",
      "kind": "cloze",
      "promptMd": "The first `at` line is usually {{a}}.",
      "blanks": [
        {
          "id": "a",
          "choices": [
            {
              "id": "the function that threw",
              "md": "the function that threw"
            },
            {
              "id": "the first function in the file",
              "md": "the first function in the file"
            },
            {
              "id": "the catch block",
              "md": "the catch block"
            }
          ]
        }
      ],
      "answer": {
        "a": "the function that threw"
      },
      "explainMd": "Newest call is on top. That is where throw ran, not where the file began."
    },
    {
      "type": "debug",
      "id": "fix-step",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> `step(\"east\")` should not throw. Print `ok`. `step(\"up\")` still throws.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "ok"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "The crash is inside `step`. Read the first `at` line."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "Only throw when the dir is unknown."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "Return a legal dir such as `\"east\"` instead of throwing."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "function step(dir) {\n  if (dir !== \"east\" && dir !== \"west\" && dir !== \"north\" && dir !== \"south\") throw new Error(\"bad\")\n  return dir\n}\nstep(\"east\")\nconsole.log(\"ok\")\nmodule.exports = { step }"
        }
      ]
    }
  ],
  "description": "Read the stack to see which call failed and why."
}
