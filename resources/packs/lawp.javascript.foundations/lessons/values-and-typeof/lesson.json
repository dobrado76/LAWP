{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 12,
  "skillIds": [
    "js.values"
  ],
  "id": "values-and-typeof",
  "courseId": "values",
  "moduleId": "kinds",
  "title": "Values and typeof",
  "blocks": [
    {
      "type": "explain",
      "md": "## Kinds you can print\n\nEvery value in JavaScript has a **kind**. The operator that reports it is `typeof`. A later desk will ask “what kind of signal is this?” before it files the record — the same question this lesson asks of a number and a string that look alike.\n\nThat is why `\"3\"` and `3` are not interchangeable. Quotes make text. A bare numeral is a number. Filing the wrong kind is how a lamp current of `\"2\"` sits in a drawer that expected `2` and then fails a later add.\n\n```javascript\ntypeof 3      // \"number\"\ntypeof \"3\"    // \"string\"\ntypeof null   // \"object\"  — leftover language bug\n```\n\n`typeof null` answering `\"object\"` is a leftover bug, not a license to poke fields on `null`. Do not treat `null` as a bag. Write `kindOf` so a hidden test can call it, and print the kind of the number `3`."
    },
    {
      "type": "predict",
      "id": "typeof-null",
      "promptMd": "`typeof null` prints which string?",
      "kind": "mcq",
      "choices": [
        {
          "id": "null",
          "md": "`null`"
        },
        {
          "id": "object",
          "md": "`object`",
          "misconceptionId": "null-is-object-ok"
        },
        {
          "id": "undefined",
          "md": "`undefined`"
        }
      ],
      "answer": "object",
      "explainMd": "`typeof null` is `\"object\"` — a leftover language bug. Do not treat `null` as a bag of fields."
    },
    {
      "type": "check",
      "id": "kind-words",
      "kind": "cloze",
      "promptMd": "`typeof 3` is {{a}}. `typeof \"3\"` is {{b}}.",
      "blanks": [
        {
          "id": "a",
          "choices": [
            {
              "id": "number",
              "md": "number"
            },
            {
              "id": "string",
              "md": "string"
            },
            {
              "id": "object",
              "md": "object"
            }
          ]
        },
        {
          "id": "b",
          "choices": [
            {
              "id": "number",
              "md": "number"
            },
            {
              "id": "string",
              "md": "string"
            },
            {
              "id": "object",
              "md": "object"
            }
          ]
        }
      ],
      "answer": {
        "a": "number",
        "b": "string"
      },
      "explainMd": "A numeral is a number. Quotes make text, even when the text looks like a digit."
    },
    {
      "type": "code",
      "id": "typeof-number",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> Return and print `typeof 3` (`number`).",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "number"
        },
        {
          "type": "ast",
          "query": "typeof"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "The starter returns a placeholder. It should report the kind of whatever value arrives."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "`typeof` is an operator, not a function you must call with extra punctuation: `typeof v`."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "`function kindOf(v) { return typeof v }` makes `kindOf(\"3\")` answer `string`."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "function kindOf(v) { return typeof v }\nconsole.log(kindOf(3))\nmodule.exports = { kindOf }"
        }
      ]
    }
  ],
  "description": "Every value has a kind: number, text, or true and false. You need this before any list."
}
