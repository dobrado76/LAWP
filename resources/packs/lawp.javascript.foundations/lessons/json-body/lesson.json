{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 18,
  "skillIds": [
    "js.http"
  ],
  "id": "json-body",
  "courseId": "talking-servers",
  "moduleId": "messages",
  "title": "JSON body parse errors",
  "blocks": [
    {
      "type": "explain",
      "md": "## Bad JSON is a throw from `res.json()`\n\n`/broken` returns `{` — not valid JSON. `res.json()` rejects. Catch that and return `\"bad-json\"`.\n\nGood `/beacons.json` still parses. The fox asked for a record and got a torn slip. Handle the tear. Do not pretend the body was `{}`."
    },
    {
      "type": "predict",
      "id": "json-throw",
      "promptMd": "`res.json()` on `{` …",
      "kind": "mcq",
      "choices": [
        {
          "id": "obj",
          "md": "Returns `{}`"
        },
        {
          "id": "throw",
          "md": "Rejects / throws a parse error"
        }
      ],
      "answer": "throw"
    },
    {
      "type": "check",
      "id": "json-empty",
      "kind": "tf",
      "promptMd": "`res.json()` on `{` returns `{}`.",
      "answer": "false",
      "explainMd": "A torn body is a parse error. Catch it. Do not invent an empty object."
    },
    {
      "type": "code",
      "id": "parse-safe",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> `safeRead(\"/broken\")` returns `\"bad-json\"`. Print it.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        },
        {
          "path": "files/beacons.json",
          "role": "fixture"
        },
        {
          "path": "files/routes.json",
          "role": "fixture"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "bad-json"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "Fetch the URL, then try to parse the body."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "Wrap `res.json()` in `try/catch`."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "On parse failure, return the string `bad-json`."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "async function safeRead(url) {\n  const res = await fetch(url)\n  try { return await res.json() }\n  catch { return \"bad-json\" }\n}\nsafeRead(\"/broken\").then((v) => console.log(v))\nmodule.exports = { safeRead }"
        }
      ]
    }
  ],
  "description": "Reading a body as data can fail. Handle the bad text."
}
