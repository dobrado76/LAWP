{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 18,
  "skillIds": [
    "js.test"
  ],
  "id": "jsdoc-contracts",
  "courseId": "craft",
  "moduleId": "ship",
  "title": "JSDoc as a checklist",
  "blocks": [
    {
      "type": "explain",
      "md": "## `@param` / `@returns` are a contract you can read\n\nThis is not TypeScript-as-product. Write the tags so a later you (or a TS pack) can check them. Node does not enforce JSDoc at runtime.\n\n`/** @param {string[]} dirs @returns {number} */` above `walkLength`. Print the length of two dirs (`2`). The hidden test looks for `@param` and `@returns` in the file text.\n\nThe fox helper’s card on the desk lists what goes in and what comes out. A missing `@returns` is a missing promise to the next shift."
    },
    {
      "type": "predict",
      "id": "jsdoc-runtime",
      "promptMd": "JSDoc tags…",
      "kind": "mcq",
      "choices": [
        {
          "id": "enforce",
          "md": "Are enforced by Node at runtime"
        },
        {
          "id": "doc",
          "md": "Document the contract; tools may check them later"
        }
      ],
      "answer": "doc"
    },
    {
      "type": "check",
      "id": "jsdoc-enforced",
      "kind": "tf",
      "promptMd": "JSDoc tags are enforced by Node when the function runs.",
      "answer": "false",
      "explainMd": "The tags are a contract you can read. This lesson’s grader only checks that the words are in the file."
    },
    {
      "type": "code",
      "id": "jsdoc-fn",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> Document and implement `walkLength`. Print `2`.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "2"
        },
        {
          "type": "ast",
          "query": "@param"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "Keep `walkLength` returning `dirs.length`. Add a JSDoc block above it."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "The block needs `@param` and `@returns`. Node will not check the types for you."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "Put @param and @returns above the function."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "/** @param {string[]} dirs @returns {number} */\nfunction walkLength(dirs) { return dirs.length }\nconsole.log(walkLength([\"east\", \"south\"]))\nmodule.exports = { walkLength }"
        }
      ]
    }
  ],
  "description": "A short comment can be a checklist of what a function owes."
}
