{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 20,
  "skillIds": [
    "js.values"
  ],
  "id": "numbers-and-nan",
  "courseId": "values",
  "moduleId": "kinds",
  "title": "Numbers and NaN",
  "blocks": [
    {
      "type": "explain",
      "md": "## Not a Number is still a number kind\n\n`Number(\"fox\")` is `NaN`. That value is the language saying “this failed to become a number.” It is still a number *kind*: `typeof NaN` is `\"number\"`. A desk that stores lamp current must catch it, or it files a broken reading as if it were a real zero.\n\nYou cannot find it with `===`. `NaN === NaN` is **false**. Comparing against the word `\"fox\"` only catches one bad input. The operator that asks the real question is `Number.isNaN`.\n\n```javascript\nNumber(\"fox\")           // NaN\ntypeof NaN              // \"number\"\nNaN === NaN             // false\nNumber.isNaN(Number(\"fox\"))  // true\n```\n\n`failedNumber(text)` must use `Number.isNaN`. Hard-coding `text === \"fox\"` is the mistake this lesson removes."
    },
    {
      "type": "predict",
      "id": "nan-equals",
      "promptMd": "`NaN === NaN` is…",
      "kind": "mcq",
      "choices": [
        {
          "id": "true",
          "md": "true"
        },
        {
          "id": "false",
          "md": "false"
        }
      ],
      "answer": "false",
      "explainMd": "NaN is never equal to anything, including itself. Use `Number.isNaN` to detect it."
    },
    {
      "type": "code",
      "id": "is-nan",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> `failedNumber(\"fox\")` returns `true` using `Number.isNaN`. Print it.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "true"
        },
        {
          "type": "ast",
          "query": "Number.isNaN"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "Comparing against the word `fox` only catches one bad input. Ask the real question instead."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "`Number(text)` gives `NaN` when the text is not numeric, and `Number.isNaN` asks about that on purpose."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "`Number.isNaN(Number(\"sky\"))` is `true`; `Number.isNaN(Number(\"7\"))` is `false`."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "function failedNumber(text) { return Number.isNaN(Number(text)) }\nconsole.log(failedNumber(\"fox\"))\nmodule.exports = { failedNumber }"
        }
      ]
    }
  ],
  "description": "Some calculations have no numeric answer. Spot NaN and do not treat it as zero."
}
