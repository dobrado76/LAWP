{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 22,
  "skillIds": [
    "js.flow"
  ],
  "id": "truth-and-if",
  "courseId": "values",
  "moduleId": "compare",
  "title": "Truth and if",
  "blocks": [
    {
      "type": "explain",
      "md": "## Some values act as “no”\n\nAn `if` does not ask “is there a value here.” It asks “is this value truthy.” Six values are falsy: `0`, `\"\"`, `null`, `undefined`, `NaN`, and `false`. A lamp at brightness `0` is off. An empty name is missing.\n\n`[]` and `\"0\"` are truthy. An empty list is still a list. The string zero is still text. Treating them as “no” is how a desk skips a real reading.\n\n```javascript\nif (0) { \"go\" } else { \"stop\" }     // \"stop\"\nif ([]) { \"go\" } else { \"stop\" }    // \"go\"\nif (\"0\") { \"go\" } else { \"stop\" }   // \"go\"\n```\n\nThe common mistake is listing the six falsy values by hand, or treating `[]` as shut because it looks empty. On Try, `gate(value)` returns `\"open\"` when the value is truthy, otherwise `\"shut\"`. `gate(0)` must print `shut`. Let the `if` do the work."
    },
    {
      "type": "predict",
      "id": "empty-array",
      "promptMd": "`if ([]) { \"go\" } else { \"stop\" }` yields…",
      "kind": "mcq",
      "choices": [
        {
          "id": "stop",
          "md": "`stop` — empty means false"
        },
        {
          "id": "go",
          "md": "`go` — an array object is truthy"
        }
      ],
      "answer": "go"
    },
    {
      "type": "check",
      "id": "falsy-set",
      "kind": "cloze",
      "promptMd": "In an `if`, {{a}} is falsy and {{b}} is truthy.",
      "blanks": [
        {
          "id": "a",
          "choices": [
            {
              "id": "0",
              "md": "0"
            },
            {
              "id": "[]",
              "md": "[]"
            },
            {
              "id": "\"0\"",
              "md": "\"0\""
            }
          ]
        },
        {
          "id": "b",
          "choices": [
            {
              "id": "0",
              "md": "0"
            },
            {
              "id": "[]",
              "md": "[]"
            },
            {
              "id": "null",
              "md": "null"
            }
          ]
        }
      ],
      "answer": {
        "a": "0",
        "b": "[]"
      },
      "explainMd": "Zero is falsy. An empty array is still an object, so it is truthy."
    },
    {
      "type": "code",
      "id": "label-truth",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> `gate(value)` returns `\"open\"` if the value is truthy, otherwise `\"shut\"`. Print `gate(0)`.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "shut"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "The starter always answers `open`. The value that arrives should decide."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "`if (value)` is enough on its own: `0` and `\"\"` are falsy, `[]` and `\"0\"` are truthy."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "`return value ? \"yes\" : \"no\"` picks between two strings in one line."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "function gate(value) { return value ? \"open\" : \"shut\" }\nconsole.log(gate(0))\nmodule.exports = { gate }"
        }
      ]
    }
  ],
  "description": "Some values act as no in a decision. An empty list is still a list."
}
