{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 25,
  "skillIds": [
    "js.values",
    "js.flow"
  ],
  "id": "transfer-classify-signal",
  "courseId": "values",
  "moduleId": "compare",
  "title": "Transfer: classify a signal",
  "mastery": {
    "requiresTransfer": true,
    "minCorrectIndependent": 1
  },
  "blocks": [
    {
      "type": "explain",
      "md": "## Name the kind, honestly\n\nThis transfer uses everything from the chapter. `classify(value)` must label a signal without trusting `typeof` alone. `typeof null` is `\"object\"` — that leftover bug will file an empty reading as a bag of fields if you check kind first.\n\nCheck `null` and `undefined` before anything else. Then `0` and `\"\"` as `\"zero\"`. Then `Array.isArray` for `\"list\"`. Then other strings as `\"text\"`. Everything else is `\"other\"`.\n\n```javascript\ntypeof null          // \"object\" — do not trust this\nclassify(null)       // \"empty\"\nclassify(0)          // \"zero\"\nclassify([])         // \"list\"\nclassify(\"east\")     // \"text\"\n```\n\nThe common mistake is `return typeof value`, which labels `null` as `object` and `[]` as `object` too. On Try, walk the labels in order and print `classify(null)` (`empty`). Hidden tests cover the other labels."
    },
    {
      "type": "predict",
      "id": "null-first",
      "promptMd": "If you write `typeof value === \"object\"` first, `null` is labelled…",
      "kind": "mcq",
      "choices": [
        {
          "id": "object",
          "md": "as an object — that is the trap",
          "misconceptionId": "null-is-object-ok"
        },
        {
          "id": "empty",
          "md": "empty only if you checked null first"
        }
      ],
      "answer": "object"
    },
    {
      "type": "code",
      "id": "classify",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> Print `classify(null)` (`empty`). Hidden tests cover the other labels.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "empty"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "Check null and undefined before typeof."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "Array.isArray tells lists apart from plain objects."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "The first line does the most work: `if (value === null || value === undefined) return \"empty\"`. Every other label follows the same shape."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "function classify(value) {\n  if (value === null || value === undefined) return \"empty\"\n  if (value === 0 || value === \"\") return \"zero\"\n  if (Array.isArray(value)) return \"list\"\n  if (typeof value === \"string\") return \"text\"\n  return \"other\"\n}\nconsole.log(classify(null))\nmodule.exports = { classify }"
        }
      ]
    }
  ],
  "description": "Use kinds of values to label a signal in a new story."
}
