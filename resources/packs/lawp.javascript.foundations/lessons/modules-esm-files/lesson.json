{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 22,
  "skillIds": [
    "js.modules"
  ],
  "id": "modules-esm-files",
  "courseId": "craft",
  "moduleId": "ship",
  "title": "export a walker, import in main",
  "blocks": [
    {
      "type": "explain",
      "md": "## Two files\n\n`walker.mjs` exports `walkLength`. `main.mjs` imports that name and prints `2` for `[\"east\", \"south\"]`. A named export is not a global. The other file must import it. The desk’s fox helper lives in one file. The night log printer lives in another. Sharing a name is an `export` / `import`, not a hope that both files see the same global.\n\nWhy it bites: writing `function walkLength` in `main.mjs` only, or printing `2` as a literal, fools your eyes and fails the hidden test. That test imports `walkLength` from `walker.mjs`, not from `main.mjs`. A missing export is a missing name.\n\n```javascript\n// walker.mjs\nexport function walkLength(dirs) { return dirs.length }\n// main.mjs\nimport { walkLength } from \"./walker.mjs\"\nconsole.log(walkLength([\"east\", \"south\"]))  // 2\n```\n\nThe common mistake is treating `export` as a global. On Try, keep the function in `walker.mjs`, import it in `main.mjs`, and print `2`."
    },
    {
      "type": "predict",
      "id": "export-name",
      "promptMd": "`export function walkLength` is…",
      "kind": "mcq",
      "choices": [
        {
          "id": "global",
          "md": "A global in every file automatically"
        },
        {
          "id": "named",
          "md": "A named export you must import"
        }
      ],
      "answer": "named"
    },
    {
      "type": "check",
      "id": "export-is-global",
      "kind": "tf",
      "promptMd": "`export function walkLength` is automatically a global in every file.",
      "answer": "false",
      "explainMd": "main.mjs must import { walkLength } from \"./walker.mjs\". The hidden test does the same."
    },
    {
      "type": "code",
      "id": "two-files",
      "engine": "javascript",
      "entry": "main.mjs",
      "promptMd": "> Export from `walker.mjs`, import in `main.mjs`, print `2`.",
      "files": [
        {
          "path": "files/walker.mjs",
          "role": "edit"
        },
        {
          "path": "files/main.mjs",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.mjs",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "2"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "Put `walkLength` in `walker.mjs` and export it. Import it in `main.mjs`."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "`walkLength` returns `dirs.length`. Print that for east then south."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "import { walkLength } from \"./walker.mjs\""
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "walker.mjs:\nexport function walkLength(dirs) { return dirs.length }\nmain.mjs:\nimport { walkLength } from \"./walker.mjs\"\nconsole.log(walkLength([\"east\", \"south\"]))"
        }
      ]
    }
  ],
  "description": "Export a walker from one file and import it in another."
}
