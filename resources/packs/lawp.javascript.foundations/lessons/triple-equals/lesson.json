{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 15,
  "skillIds": [
    "js.values"
  ],
  "id": "triple-equals",
  "courseId": "values",
  "moduleId": "compare",
  "title": "Triple equals",
  "blocks": [
    {
      "type": "explain",
      "md": "## `===` does not convert\n\n`==` converts before it compares. That is why `0 == \"\"` is `true`: empty string becomes `0`. `===` refuses that conversion. A signal of `0` (off) is not a missing name `\"\"`. A desk that treats those as the same name files a dark lamp as an unnamed beacon.\n\nPrefer `===` and `!==` unless you can name the coercion you want. The starter on this desk still uses `==`. That is the habit to drop.\n\n```javascript\n0 == \"\"    // true  — == converted the string\n0 === \"\"   // false — different kinds\n0 !== \"\"   // true\n```\n\nThe common mistake is keeping `==` because it “looks like equals.” On Try, print whether `0` and `\"\"` are **not** strictly equal. Use `===` or `!==`. Do not keep `==`."
    },
    {
      "type": "predict",
      "id": "double-trap",
      "promptMd": "`0 == \"\"` is true. Is `0 === \"\"` also true?",
      "kind": "mcq",
      "choices": [
        {
          "id": "yes",
          "md": "Yes — empty is empty",
          "misconceptionId": "double-equals-is-fine"
        },
        {
          "id": "no",
          "md": "No — different kinds stay different"
        }
      ],
      "answer": "no"
    },
    {
      "type": "code",
      "id": "strict",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> Print `true` because `0` and `\"\"` are not strictly equal. Use `!==` or `===`.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "true",
          "misconceptionId": "double-equals-is-fine"
        },
        {
          "type": "ast",
          "query": "==="
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "The starter uses `==`, which converts before it compares. That is the habit this lesson removes."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "`===` and `!==` compare kind and value together, so `0 === \"\"` is false."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "`function same(a, b) { return a === b }` answers \"the same\"; you want the opposite of that."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "function notStrictSame(a, b) { return !(a === b) }\nconsole.log(notStrictSame(0, \"\"))\nmodule.exports = { notStrictSame }"
        }
      ]
    }
  ],
  "description": "Strict equality compares value and kind. Loose equality will surprise you."
}
