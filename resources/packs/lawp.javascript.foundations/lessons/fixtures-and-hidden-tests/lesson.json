{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 22,
  "skillIds": [
    "js.test"
  ],
  "id": "fixtures-and-hidden-tests",
  "courseId": "craft",
  "moduleId": "prove",
  "title": "Fixtures and hidden tests",
  "blocks": [
    {
      "type": "explain",
      "md": "## You write a test the hidden runner also runs\n\n`cases.json` is a fixture: a list of `{ dirs, n }` records. Read that file and assert `walkLength` for each row. You do not type the cases into the test by hand if the file already holds them.\n\nPrint `ok` when every fixture case passes. A hidden test is a file you cannot edit. The grader runs it after your program.\n\nThe desk keeps walk lengths in `cases.json`. The fox route `[\"east\"]` must be length `1` even when no one is watching the stage."
    },
    {
      "type": "predict",
      "id": "hidden-role",
      "promptMd": "A hidden test is…",
      "kind": "mcq",
      "choices": [
        {
          "id": "secret-ui",
          "md": "A second editor tab"
        },
        {
          "id": "grade",
          "md": "A file the grader runs that you cannot edit"
        }
      ],
      "answer": "grade"
    },
    {
      "type": "check",
      "id": "fixture-role",
      "kind": "cloze",
      "promptMd": "A fixture is a {{a}} the test reads. A hidden test is a file the {{b}} runs.",
      "blanks": [
        {
          "id": "a",
          "choices": [
            {
              "id": "data file",
              "md": "data file"
            },
            {
              "id": "editor theme",
              "md": "editor theme"
            },
            {
              "id": "bundler",
              "md": "bundler"
            }
          ]
        },
        {
          "id": "b",
          "choices": [
            {
              "id": "grader",
              "md": "grader"
            },
            {
              "id": "browser only",
              "md": "browser only"
            },
            {
              "id": "fox",
              "md": "fox"
            }
          ]
        }
      ],
      "answer": {
        "a": "data file",
        "b": "grader"
      },
      "explainMd": "cases.json holds the rows. The hidden test is extra proof you cannot rewrite."
    },
    {
      "type": "code",
      "id": "fixture-cases",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> `walkLength` plus a loop over `cases.json`. Print `ok`.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        },
        {
          "path": "files/cases.json",
          "role": "fixture"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "ok"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "Two jobs: make `walkLength` correct, and prove it against every row in `cases.json` rather than one example you typed."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "Read the fixture with `fs`, `JSON.parse` it, and assert `walkLength(c.dirs)` equals `c.n` for each row."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "`for (const c of rows) assert.strictEqual(walkLength(c.dirs), c.n)` fails loudly on the first bad row."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "const fs = require(\"fs\")\nconst assert = require(\"assert\")\nfunction walkLength(dirs) { return dirs.length }\nfor (const c of JSON.parse(fs.readFileSync(\"cases.json\", \"utf8\"))) {\n  assert.strictEqual(walkLength(c.dirs), c.n)\n}\nconsole.log(\"ok\")\nmodule.exports = { walkLength }"
        }
      ]
    }
  ],
  "description": "Hidden tests use fixtures. Your file is the thing they call."
}
