{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 20,
  "skillIds": [
    "js.functions",
    "js.arrays"
  ],
  "id": "signal-log",
  "courseId": "data",
  "moduleId": "records",
  "title": "Signal log",
  "creation": {
    "id": "signal-log",
    "step": 1,
    "briefMd": "logBeacon returns a tagged line the hidden tests also run."
  },
  "blocks": [
    {
      "type": "explain",
      "md": "## A log you keep\n\n`logBeacon(name)` should return `beacon:name` — a tagged line, not a side effect. Print is how you *see* the line. Return is how a later caller *keeps* it. Hidden tests call your function with other names. Two literal `console.log(\"beacon:north\")` lines would paint the notebook and still fail those tests.\n\nWhy it bites: a function that only prints and returns nothing hands `undefined` to the next desk. The creation step that stores the line files an empty slot. A function that returns the bare name files `west` when the board expected `beacon:west`.\n\n```javascript\nfunction logBeacon(name) {\n  return `beacon:${name}`\n}\nlogBeacon(\"north\")  // \"beacon:north\"\nlogBeacon(\"east\")   // \"beacon:east\"\n```\n\nOn Try, `logBeacon(name)` returns `beacon:name`. Print north then east, one per line. Use the argument — do not only print two literals. A template literal does the tag in one expression; quotes would keep the letters `${name}` if you are not careful."
    },
    {
      "type": "predict",
      "id": "return-tag",
      "promptMd": "If `logBeacon` only prints and returns nothing, a later test that reads the return gets…",
      "kind": "mcq",
      "choices": [
        {
          "id": "line",
          "md": "The printed line"
        },
        {
          "id": "undef",
          "md": "`undefined`",
          "misconceptionId": "print-is-return"
        }
      ],
      "answer": "undef"
    },
    {
      "type": "code",
      "id": "log-fn",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> `logBeacon(name)` returns `beacon:name`. Print north then east, one per line.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "beacon:north\nbeacon:east"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "The starter returns the bare name, so the printed lines are missing their tag."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "Build the tag inside the function, so every caller gets the same shape."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "A template literal does it in one line: `` return `beacon:${name}` ``."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "function logBeacon(name) { return `beacon:${name}` }\nconsole.log(logBeacon(\"north\"))\nconsole.log(logBeacon(\"east\"))\nmodule.exports = { logBeacon }"
        }
      ]
    }
  ],
  "description": "Build a small log of signals you can keep working on."
}
