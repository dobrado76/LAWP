{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 4,
  "estimatedMinutes": 18,
  "skillIds": [
    "js.arrays"
  ],
  "id": "arrays-index",
  "courseId": "data",
  "moduleId": "lists",
  "title": "Index vs length",
  "blocks": [
    {
      "type": "explain",
      "md": "## A list of directions\n\nAn array is a numbered row. Index `0` is the first item. `dirs.length` is how many items sit in the row. The last item is at `length - 1`, not at `length`. Asking for `dirs[dirs.length]` is asking for a slot that was never written — you get `undefined`, not a wrap-around.\n\nWhy it bites: a fox that reads `dirs[3]` from `[\"east\", \"east\", \"south\"]` tries to `Player.move(undefined)` and stands still, one step short of the beacon at (2, 1). Hard-coding `\"south\"` as the last heading also fails the moment a hidden test hands you `[\"west\"]`.\n\n```javascript\nconst dirs = [\"east\", \"east\", \"south\"]\ndirs[0]                // \"east\"\ndirs.length            // 3\ndirs[dirs.length - 1]  // \"south\"\ndirs[dirs.length]      // undefined\n```\n\n`lastDir(dirs)` must return the last heading with `dirs[dirs.length - 1]` — not a hardcoded `\"south\"`. Then walk every string in `dirs` with a for…of, forEach, or indexed loop. Do not paste three separate `Player.move(\"…\")` calls."
    },
    {
      "type": "predict",
      "id": "last-index",
      "promptMd": "For `[\"east\", \"south\"]`, the last index is…",
      "kind": "mcq",
      "choices": [
        {
          "id": "2",
          "md": "`2` — same as length",
          "misconceptionId": "off-by-one-inclusive"
        },
        {
          "id": "1",
          "md": "`1` — length minus one"
        }
      ],
      "answer": "1"
    },
    {
      "type": "code",
      "id": "walk-dirs",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> `lastDir(dirs)` returns the last heading with `dirs[dirs.length - 1]` — not a hardcoded `\"south\"`. Then walk every string in `dirs` (for…of / forEach / indexed loop). Do not paste three separate `Player.move(\"…\")` calls.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "The last slot is `length - 1`, not `length`. Then walk the list instead of one pasted move."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "Write `lastDir` first so the hidden test can call it with other lists."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "`for (const dir of dirs) { … }` hands you each string in turn."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "const dirs = [\"east\", \"east\", \"south\"]\nfunction lastDir(list) {\n  return list[list.length - 1]\n}\nfor (const dir of dirs) Player.move(dir)\nmodule.exports = { dirs, lastDir }"
        }
      ],
      "play": {
        "api": "player-v1",
        "playerId": "fox",
        "world": {
          "parts": [
            {
              "id": "fox",
              "type": "fox",
              "props": {
                "x": 0,
                "y": 0,
                "rot": 0,
                "label": "Fox"
              }
            },
            {
              "id": "beacon",
              "type": "beacon",
              "props": {
                "x": 2,
                "y": 1,
                "label": "Beacon"
              }
            }
          ],
          "connections": [],
          "actions": [],
          "rules": [],
          "view": {
            "kind": "grid",
            "grid": {
              "cols": 5,
              "rows": 5,
              "floor": "floor-stone"
            },
            "assetMap": {
              "fox": "fox.png",
              "owl": "owl.png",
              "robot": "robot.png",
              "beacon": "beacon.png",
              "rock": "rock.png",
              "wall": "wall.png",
              "crate": "crate.png",
              "tree": "tree.png",
              "bush": "bush.png",
              "barrel": "barrel.png",
              "coin": "coin.png",
              "key": "key.png",
              "glint": "glint.png",
              "gem": "gem.png",
              "star": "star.png",
              "scroll": "scroll.png",
              "chest": "chest.png",
              "flag": "flag.png",
              "spike": "spike.png",
              "fire": "fire.png",
              "hole": "hole.png"
            }
          }
        },
        "goal": {
          "all": [
            {
              "path": "fox.x",
              "op": "eq",
              "value": 2
            },
            {
              "path": "fox.y",
              "op": "eq",
              "value": 1
            }
          ]
        }
      }
    }
  ],
  "description": "Length is how many items. Index is the place of one item."
}
