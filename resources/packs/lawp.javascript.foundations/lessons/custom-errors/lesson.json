{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 20,
  "skillIds": [
    "js.errors"
  ],
  "id": "custom-errors",
  "courseId": "errors",
  "moduleId": "recover",
  "title": "class RouteError",
  "blocks": [
    {
      "type": "explain",
      "md": "## Name the failure\n\nA **custom error class** names a kind of failure so `catch` can tell it from the rest. `class RouteError extends Error {}` lets you ask `e instanceof RouteError`. A generic `Error` is a shrug: blocked path, missing file, and bad math all look the same. A named class is a signal the desk can branch on.\n\nA `RouteError` is still an `Error`. `e instanceof Error` stays true because `extends` keeps the subclass in that family. When the path is blocked, the fox throws `RouteError(\"blocked\")`. The desk can tell a blocked route from a missing file and file the right night note. Throw a plain `Error` and the hidden check cannot see the kind.\n\n```javascript\nclass RouteError extends Error {}\nfunction fail() {\n  throw new RouteError(\"blocked\")\n}\ntry { fail() } catch (e) {\n  e instanceof RouteError  // true\n  e instanceof Error       // true — subclasses still are Errors\n  e.message                // \"blocked\"\n}\n```\n\nA common mistake is to keep `throw new Error(\"blocked\")` after defining the class, so `instanceof RouteError` is false. On Try, define `RouteError`. `fail()` throws one with message `blocked`. Catch it and print `blocked`."
    },
    {
      "type": "predict",
      "id": "instanceof",
      "promptMd": "`throw new RouteError(\"blocked\")` then `e instanceof Error` is…",
      "kind": "mcq",
      "choices": [
        {
          "id": "false",
          "md": "false — it is a different type"
        },
        {
          "id": "true",
          "md": "true — subclasses still are Errors"
        }
      ],
      "answer": "true"
    },
    {
      "type": "check",
      "id": "still-error",
      "kind": "cloze",
      "promptMd": "`new RouteError` is {{a}} an Error.",
      "blanks": [
        {
          "id": "a",
          "choices": [
            {
              "id": "still",
              "md": "still"
            },
            {
              "id": "never",
              "md": "never"
            },
            {
              "id": "only if you wrap it",
              "md": "only if you wrap it"
            }
          ]
        }
      ],
      "answer": {
        "a": "still"
      },
      "explainMd": "extends Error keeps the subclass in the Error family. instanceof Error is true."
    },
    {
      "type": "code",
      "id": "route-error",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> Define `RouteError`. `fail()` throws one with message `blocked`. Catch it and print `blocked`.",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "blocked"
        },
        {
          "type": "ast",
          "query": "class RouteError"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "Make a class that extends `Error`."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "`fail` should `throw new RouteError(\"blocked\")`, not a plain Error."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "Catch it and print `e.message`."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "class RouteError extends Error {}\nfunction fail() { throw new RouteError(\"blocked\") }\ntry { fail() } catch (e) { console.log(e.message) }\nmodule.exports = { RouteError, fail }"
        }
      ]
    }
  ],
  "description": "Make your own error type so the catch can tell stories apart."
}
