{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.javascript.foundations",
  "taskRev": 2,
  "estimatedMinutes": 20,
  "skillIds": [
    "js.objects"
  ],
  "id": "json-roundtrip",
  "courseId": "data",
  "moduleId": "records",
  "title": "JSON round-trip",
  "blocks": [
    {
      "type": "explain",
      "md": "## Text in, object out, text again\n\n`JSON.parse` turns a string into an object. Bad text throws a `SyntaxError` — there is no quiet `{}` fallback. `JSON.stringify` goes the other way. Here the JSON arrives as a string already, held in `raw`. Reading files comes later, in the Node track; today the only question is what the text becomes.\n\nWhy it bites: returning the raw string prints `{ \"name\": \"north\", \"kind\": \"beacon\" }` instead of the name. A later desk that expected `\"north\"` files the whole blob. Swallowing a parse error and returning `undefined` hides a broken payload. A round-trip is also not always lossless: `undefined`, functions, and `Map` values do not survive `stringify`.\n\n```javascript\nconst raw = '{ \"name\": \"north\", \"kind\": \"beacon\" }'\nJSON.parse(raw).name     // \"north\"\nJSON.parse(\"{\")          // throws SyntaxError\nJSON.stringify({ a: undefined })  // \"{}\" — field dropped\n```\n\n`readName(text)` must parse the JSON in `text` and return the `name` field. Print `readName(raw)` (`north`). Hidden tests also send `{\"name\":\"west\"}` and a broken `\"{\"` that must throw, not return undefined."
    },
    {
      "type": "predict",
      "id": "parse-throw",
      "promptMd": "`JSON.parse(\"{\")` does what?",
      "kind": "mcq",
      "choices": [
        {
          "id": "empty",
          "md": "Returns `{}`"
        },
        {
          "id": "throw",
          "md": "Throws a SyntaxError"
        }
      ],
      "answer": "throw"
    },
    {
      "type": "check",
      "id": "stringify-undefined",
      "kind": "tf",
      "promptMd": "`JSON.stringify({ a: undefined })` keeps the `a` field.",
      "answer": "false",
      "explainMd": "It gives `{}`. Fields set to undefined, and functions, are dropped on the way out."
    },
    {
      "type": "code",
      "id": "read-signal",
      "engine": "javascript",
      "entry": "main.js",
      "promptMd": "> `readName(text)` parses the JSON in `text` and returns the `name` field. Print `readName(raw)` (`north`).",
      "files": [
        {
          "path": "files/main.js",
          "role": "edit"
        },
        {
          "path": "files/hidden.test.js",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "north"
        },
        {
          "type": "ast",
          "query": "JSON.parse"
        },
        {
          "type": "js-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "The starter hands the raw text straight back, so you are printing JSON instead of a name."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "`JSON.parse(text)` gives you an object; after that it is an ordinary field read."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "`JSON.parse('{\"kind\":\"beacon\"}').kind` is `\"beacon\"`."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "const raw = '{ \"name\": \"north\", \"kind\": \"beacon\" }'\nfunction readName(text) {\n  return JSON.parse(text).name\n}\nconsole.log(readName(raw))\nmodule.exports = { readName }"
        }
      ]
    }
  ],
  "description": "Turn a record into text and back again without losing the shape."
}
