# Bundled cartridges

Each subject-matter is `<packId>/` with `pack.json` and `lessons/<lessonId>/lesson.json` plus that lesson’s `files/` and `assets/`. First bundled proof: `lawp.circuits.basics` (`world-v1`, no spawn). Same layout as a user-installed zip. See [docs/CONTENT_MODEL.md](../../docs/CONTENT_MODEL.md).

Do not commit progress, notes, or workspaces here.
