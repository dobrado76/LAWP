{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.python.foundations",
  "taskRev": 2,
  "estimatedMinutes": 12,
  "skillIds": [
    "python.types.names"
  ],
  "id": "names-and-values",
  "courseId": "values",
  "moduleId": "kinds",
  "title": "Names hold values",
  "blocks": [
    {
      "type": "explain",
      "md": "## A name is a sticky note\n\nA name is a sticky note you press onto a value once the work is done. The note does not do the arithmetic. It remembers the result so the rest of the program can use it without repeating the calculation.\n\nThis bites when you print a literal instead of a name. The screen looks right once, but nothing stored the answer, so the next line cannot use it and a later change leaves a stale digit on the screen.\n\n```python\ntotal = 3 + 4\nprint(total)     # 7 — because total holds the result\n```\n\nA common mistake is treating `print` as the storage. Printing shows a value and then forgets it. The exercise asks you to add `3 + 4` into the name `total` and print that name, so the `7` comes from the binding rather than from a digit you typed."
    },
    {
      "type": "predict",
      "id": "what-prints",
      "promptMd": "After `total = 3 + 4`, what does `print(total)` show?",
      "kind": "mcq",
      "choices": [
        {
          "id": "expr",
          "md": "The text `3 + 4`"
        },
        {
          "id": "seven",
          "md": "`7`"
        }
      ],
      "answer": "seven",
      "explainMd": "The right-hand side runs first and produces `7`. The name is then bound to that finished value, so printing the name shows `7`, not the expression that made it."
    },
    {
      "type": "check",
      "id": "binding-words",
      "kind": "cloze",
      "promptMd": "In `total = 3 + 4`, the {{a}} runs first and the {{b}} remembers the result.",
      "blanks": [
        {
          "id": "a",
          "choices": [
            {
              "id": "expression",
              "md": "expression"
            },
            {
              "id": "name",
              "md": "name"
            },
            {
              "id": "print",
              "md": "print"
            }
          ]
        },
        {
          "id": "b",
          "choices": [
            {
              "id": "name",
              "md": "name"
            },
            {
              "id": "expression",
              "md": "expression"
            },
            {
              "id": "screen",
              "md": "screen"
            }
          ]
        }
      ],
      "answer": {
        "a": "expression",
        "b": "name"
      },
      "explainMd": "Python evaluates the right-hand side first and produces one finished value. Only then is the name on the left bound to it, which is why printing the name later shows `7` rather than the expression that made it."
    },
    {
      "type": "code",
      "id": "make-total",
      "engine": "python",
      "entry": "main.py",
      "promptMd": "> Add `3 + 4` into the name `total`, then print that name. Output should be `7`.",
      "files": [
        {
          "path": "files/main.py",
          "role": "edit"
        },
        {
          "path": "files/hidden_test.py",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "7"
        },
        {
          "type": "ast",
          "query": "total"
        },
        {
          "type": "python-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "The starter prints a digit directly. Nothing is stored, so nothing can be reused."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "Bind the sum to a name first: `name = value`. Then print the name."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "`parts = 2 + 5` then `print(parts)` shows `7` from the binding, not from a typed digit."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "total = 3 + 4\nprint(total)"
        }
      ]
    }
  ],
  "description": "A name holds a value you can use again later."
}
