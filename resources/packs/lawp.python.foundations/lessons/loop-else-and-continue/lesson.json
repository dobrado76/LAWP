{
  "kind": "lesson",
  "schemaVersion": 1,
  "packId": "lawp.python.foundations",
  "taskRev": 1,
  "estimatedMinutes": 17,
  "skillIds": [
    "python.control.loops"
  ],
  "id": "loop-else-and-continue",
  "courseId": "flow",
  "moduleId": "repeat",
  "title": "Skip a pass, or finish without finding",
  "blocks": [
    {
      "type": "explain",
      "md": "## Two words that shape a search\n\n`continue` abandons the current pass and jumps straight to the next one, which keeps a filter from burying the real work in another level of indentation. Python also lets a loop carry an `else`, and that `else` runs when the loop finished normally — that is, when no `break` fired.\n\nThis bites because `for ... else` reads like \"otherwise\" and means \"no break\". Attached to a search loop it is genuinely useful: the `else` is the not-found case, and you no longer need a `found = False` flag that someone will forget to reset. Read it as `for ... then-if-nothing-broke` and it stops being strange.\n\n```python\nfor n in range(1, 6):\n    if n % 2 == 0:\n        continue          # skip the evens, keep looping\n    print(n)              # 1 3 5\n\nfor n in range(3):\n    if n == 99:\n        break\nelse:\n    print(\"no break fired\")\n```\n\nA common mistake is letting a search loop run to the end after it has already found its answer, so a later value quietly overwrites the first match. Once you have what you came for, `break` — and put the not-found answer in the `else`. The exercise asks for the *first* odd multiple of seven, so stopping at the right moment is the whole task."
    },
    {
      "type": "predict",
      "id": "else-after-break",
      "promptMd": "A `for` loop hits `break` on its second pass. Does its `else` block run?",
      "kind": "mcq",
      "choices": [
        {
          "id": "yes",
          "md": "Yes — `else` runs after every loop"
        },
        {
          "id": "no",
          "md": "No — `else` only runs when no `break` fired"
        }
      ],
      "answer": "no",
      "explainMd": "A loop `else` is really a \"completed without breaking\" clause. Because the loop was cut short by `break`, the `else` is skipped. That is exactly what makes the pattern useful for searches: the `else` is the branch that means \"I looked at everything and found nothing\"."
    },
    {
      "type": "check",
      "id": "continue-exits",
      "kind": "tf",
      "promptMd": "`continue` ends the loop the same way `break` does.",
      "answer": "false",
      "explainMd": "It does not. `continue` only abandons the rest of the current pass; the loop then asks for the next value and carries on. `break` leaves the loop for good. Confusing the two turns a filter into an early exit, so a scan that was meant to skip blank lines stops at the first one instead."
    },
    {
      "type": "code",
      "id": "first-odd-multiple",
      "engine": "python",
      "entry": "main.py",
      "promptMd": "> Finish `first_odd_multiple(limit)` so it returns the first **odd** multiple of 7 below `limit`, skipping even numbers with `continue`, and returns `0` from the loop `else` when there is none. `first_odd_multiple(30)` is `7`.",
      "files": [
        {
          "path": "files/main.py",
          "role": "edit"
        },
        {
          "path": "files/hidden_test.py",
          "role": "hidden-test"
        }
      ],
      "checks": [
        {
          "type": "stdout",
          "equals": "7"
        },
        {
          "type": "ast",
          "query": "continue"
        },
        {
          "type": "python-assert"
        }
      ],
      "hintLadder": [
        {
          "level": 1,
          "kind": "concept",
          "md": "The starter looks at every number and keeps overwriting its answer, so it reports the last match instead of the first."
        },
        {
          "level": 2,
          "kind": "concept",
          "md": "Skip the evens with `continue`, and `break` out the moment you find a match. Put the `0` in an `else` attached to the `for`."
        },
        {
          "level": 3,
          "kind": "concept",
          "md": "`for c in text:` with `if c == \" \": continue` skips the spaces; a later `break` plus `else` handles \"searched everything, found nothing\"."
        },
        {
          "level": 4,
          "kind": "assist",
          "md": "def first_odd_multiple(limit):\n    for n in range(1, limit):\n        if n % 2 == 0:\n            continue\n        if n % 7 == 0:\n            found = n\n            break\n    else:\n        found = 0\n    return found\n\n\nprint(first_odd_multiple(30))"
        }
      ]
    }
  ],
  "description": "Skip a turn with continue, and run the loop else only when nothing broke."
}
